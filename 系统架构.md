# 信息发布安全监管系统架构文档

> 更新时间：2026-07-02  
> 对齐代码目录：`D:\project02\monitor-platform`、`D:\project02\info-publish-client`、`D:\project02\publish-gateway`、`D:\project02\terminal-gateway`  
> 说明：本文以 `monitor-platform` 为管控平台基准，不以 `monitor-platform-new` 为依据。

## 一、系统总览

本系统用于对交通情报板、LED 信息发布屏等终端的发布链路进行安全监管，覆盖发布来源校验、加密转发、内容审计、设备管理、链路下发、告警处置、UKey/国密认证和前端实时推送。

| 工程 | 目录 | 部署环境 | 角色 | 主要端口 |
|------|------|----------|------|----------|
| 发布客户端 | `info-publish-client` | Windows，与 Sigma 发布端同机 | 来源校验、ARP/透明代理、内容预审、安全发布签名 | 7081 |
| 发布网关 | `publish-gateway/gateway-udp-proxy` | Linux | UDP/TCP 转发、协议解析、内容上报、加密、安全包校验 | 8092，18092 |
| 终端网关 | `terminal-gateway/gateway-udp-proxy`、`gateway-device-core`、`gateway-device-transport` | Linux | 解密转发、Message 分片重组、设备协议编排、Netty 传输、情报板探测、屏幕快照 | 8093 |
| 管控平台 | `monitor-platform` | Linux Docker | 设备、内容、告警、规则、链路、UKey、权限、注册、WebSocket | 多服务 |

## 二、总体链路

```text
Sigma 发布端
  |
  | UDP 明文，或由 Windows 客户端透明代理/签名转发
  v
info-publish-client ---- HTTP 来源校验/签名记录 ----> publish-gateway
  |                                                |
  | ARP 绑定、进程校验、WinDivert、内容预审          | 协议解析、内容上报、SM4/VAuth 加密
  v                                                v
Windows 发布主机                             terminal-gateway
                                                   |
                                                   | 解密后 UDP/TCP 转发、状态探测、快照上报
                                                   v
                                             情报板 / LED 终端

info-publish-client / publish-gateway / terminal-gateway
  |
  | 注册、心跳、内容、状态、告警
  v
monitor-platform
```

### 2.1 主要交互

| 调用方 | 被调用方 | 协议 | 说明 |
|--------|----------|------|------|
| publish-gateway | info-publish-client | HTTP | `/security/validate-source` 来源合法性校验 |
| publish-gateway | terminal-gateway | UDP | Message 协议封装后的加密数据 |
| publish-gateway | terminal-gateway | MQTT | 通过 EMQX Broker 下发配置和命令 |
| terminal-gateway | 情报板 | UDP/TCP | 解密后的实际终端通信 |
| publish-gateway | monitor-content | HTTP | 内容解析后上报检测 |
| publish-gateway | monitor-log | HTTP | 发布日志和诊断事件上报 |
| terminal-gateway | monitor-device | HTTP | 情报板连通性批量状态上报 |
| terminal-gateway | monitor-log | HTTP | 链路日志上报 |
| 各客户端/网关 | monitor-device / registry-server | HTTP/MQTT | 自动注册、心跳、服务发现；公网 MQTT 场景由设备主动连接 Broker 后上报 |
| monitor-forward | publish-gateway / terminal-gateway | HTTP/MQTT | 下发或更新代理规则 |
| monitor-alarm / monitor-content | monitor-websocket / Redis | Redis/WebSocket | 告警、内容状态等前端实时推送 |
| monitor-alarm | monitor-log | HTTP | 告警日志记录 |

## 三、管控平台架构

### 3.1 Maven 模块

`monitor-platform/pom.xml` 当前包含以下模块：

| 模块 | 职责 |
|------|------|
| `monitor-platform-device` | 统一设备管理、情报板批量导入、心跳、状态、位置、命令 |
| `monitor-platform-forward` | 转发链路管理、网关与情报板映射、链路下发 |
| `monitor-platform-content` | 内容接收、预审、本地/AI 检测、内容记录 |
| `monitor-platform-Ukey` | VAuth 服务端、证书、UKey 在线状态、登录、临时授权 |
| `monitor-platform-alarm` | 告警接收、待处理、处置、统计、与设备/内容关联 |
| `monitor-platform-rule` | 检测规则、敏感词、阈值、规则统计 |
| `monitor-platform-common` | 公共实体、工具、常量 |
| `monitor-platform-log` | 链路日志、操作日志、发布日志、诊断事件管理 |
| `monitor-platform-websocket` | WebSocket 推送服务 |
| `monitor-platform-gateway` | Spring Cloud Gateway 统一入口 |
| `monitor-platform-role` | 用户、角色、权限、区域、系统参数、操作日志 |
| `monitor-platform-provider` | 服务提供者能力，供外部组件集成 |
| `monitor-platform-registry-client` | 自动注册 Starter |
| `monitor-platform-registry-server` | 服务实例注册、心跳、发现、客户端配置、网关部署 |
| `monitor-platform-register-service` | 注册服务扩展模块，当前 docker-compose 未单独部署 |
| `monitor-platform-monolith` | 单体部署形态，默认端口 8080 |
| `monitor-platform-mqtt-core` | MQTT 公共协议模块，供发布网关和链路管理使用 |

### 3.2 当前 Docker 部署

以 `monitor-platform/deploy/docker-compose.yml` 为准：

| 服务 | 容器 | 端口 | 说明 |
|------|------|------|------|
| EMQX | `emqx` | 1883/8883/18083 | MQTT Broker，支持设备 MQTT 通信 |
| Nacos | `nacos` | 18848/19848/19849/9848/9849 | 注册中心、配置中心 |
| MySQL | `mysql` | 23306 | 业务库和 Nacos 配置库，镜像为 MySQL 5.7 |
| Redis | `redis` | 26379 | 缓存、推送辅助 |
| MinIO | `minio` | 19000/19002 | 内容对象存储 |
| MinIO Init | `minio-init` | - | MinIO 初始化，创建 Bucket 和用户 |
| Nginx | `monitor-nginx` | 80/443 | 前端静态资源、反向代理 |
| Gateway | `monitor-gateway` | 8060 | API 网关 |
| Device | `monitor-device` | 8062 | 设备管理 |
| UKey | `monitor-ukey` | 8063 | 国密认证、证书与 UKey |
| Alarm | `monitor-alarm` | 8064 | 告警服务 |
| Content | `monitor-content` | host 网络，业务端口由配置决定 | 内容检测服务 |
| Rule | `monitor-rule` | 8066 | 规则服务 |
| WebSocket | `monitor-websocket` | 8070 | 前端实时推送 |
| Log | `monitor-log` | 8071 | 日志服务，链路/操作/发布日志和诊断事件 |

运行态备注：2026-07-01 已将 `192.168.1.25` 现场微服务部署的业务库与 Nacos 配置库连接切到 `192.168.1.31:3306`；本机 `mysql:23306` 仍是 docker-compose 中保留的 MySQL 组件端口，不作为当前业务服务连接目标。
| Forward | `monitor-forward` | 8067 | 链路管理 |
| Role | `monitor-role` | 8068 | 权限管理 |
| Registry Server | `monitor-registry-server` | 8069 | 注册服务 |

### 3.3 技术栈

| 类别 | 当前使用 |
|------|----------|
| Java | 1.8 |
| Spring Boot | 2.7.18（monitor-platform）、2.7.14（terminal-gateway） |
| Spring Cloud | 2021.0.8 |
| Spring Cloud Alibaba | 2021.0.5.0 |
| ORM | MyBatis Plus 3.5.3.1 |
| 数据库驱动 | mysql-connector-java 8.0.33（monitor-platform）、mysql-connector-j 8.4.0（terminal-gateway） |
| 数据库部署 | docker-compose 使用 MySQL 5.7 |
| 消息/缓存 | Redis、RabbitMQ 依赖存在，当前编排中部署 Redis |
| MQTT Broker | EMQX 5.4.0 |
| MQTT 客户端 | Eclipse Paho MQTT v3 1.2.5 |
| 对象存储 | MinIO 8.5.9 |
| 网络框架 | Netty 4.2.15.Final（terminal-gateway） |
| 本地访问 | JNA 5.12.1+（Java Native Access） |
| 数据库版本管理 | Liquibase 4.20.0 |
| API 文档 | Knife4j 2.0.2 |
| 工具类 | Hutool 5.8.23、FastJSON2 2.0.43、Guava 33.6.0-jre |
| 加密库 | BouncyCastle 1.70（SM2 证书解析） |

## 四、核心业务模块

### 4.1 设备管理

`monitor-platform-device` 提供统一设备入口。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| GET | `/device/unified/tree` | 设备树 |
| GET | `/device/unified/map-points` | 地图点位 |
| POST | `/device/unified/info-board/batch` | 情报板批量导入 |
| GET | `/device/unified/page` | 设备分页 |
| GET | `/device/unified/{deviceType}/{deviceId}/detail` | 按类型和 ID 查询详情 |
| POST | `/device/unified/batch-refresh` | 批量刷新状态 |
| POST | `/device/unified/batch-export` | 批量导出 |
| POST | `/device/unified/command` | 设备命令 |
| GET | `/device/unified/statistics` | 设备统计 |
| POST | `/device/unified/heartbeat` | 设备心跳 |
| GET | `/device/unified/location-by-ip` | 按 IP 查询位置 |
| POST | `/device/unified/batch-status` | 终端网关批量状态上报 |
| POST | `/device/unified/updateLocation` | 更新位置 |

网关转发配置接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/device/gateway/forward-config/deploy` | 部署单条转发配置 |
| POST | `/device/gateway/forward-config/batch-deploy/{gatewaySn}` | 批量部署到网关 |
| POST | `/device/gateway/forward-config/control/{gatewaySn}/{configId}/{action}` | 控制配置启停 |
| GET | `/device/gateway/forward-config/list/{gatewaySn}` | 查询网关配置 |
| POST | `/device/gateway/forward-config/test/{gatewaySn}/{configId}` | 测试配置 |

### 4.2 内容检测

`monitor-platform-content` 负责接收网关解析出的内容、终端快照和文件引用，并执行本地规则或 AI 检测。

主要能力：

- 内容接收与记录。
- 图片、文本、视频本地预审。
- 规则服务联动：启用规则、敏感词、阈值。
- 告警服务联动：异常内容生成告警。
- MinIO 对象存储引用。
- 情报板监控列表管理。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/content/receive` | 接收内容 |
| POST | `/content/board-monitor-list` | 情报板监控列表 |
| GET | `/content/detail/{contentId}` | 内容详情 |
| GET | `/content/latest-by-board` | 按情报板查询最新内容 |
| GET | `/content/list` | 内容列表 |
| GET | `/content/violations` | 违规内容 |
| POST | `/content/stop` | 停止监控 |
| POST | `/content/resume` | 恢复监控 |
| POST | `/content/reloadWords` | 重载敏感词 |
| DELETE | `/content/delete/{contentId}` | 删除内容 |
| POST | `/content/detection/detect` | 内容检测 |

注意：当前 docker-compose 中 `monitor-content` 使用 `network_mode: host`，其对 MySQL、Redis、Nacos、MinIO 和其他服务的访问地址通过 `CONTENT_*` 环境变量覆盖。

### 4.3 告警管理

`monitor-platform-alarm` 负责告警接收、查询、处置和统计，告警来源包括内容检测异常、设备离线、链路异常、认证或来源校验异常。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/alarm/receive` | 接收告警 |
| POST | `/alarm/handle` | 处置告警 |
| GET | `/alarm/page` | 告警分页 |
| GET | `/alarm/detail/{id}` | 告警详情 |
| GET | `/alarm/recent` | 最近告警 |
| GET | `/alarm/pending` | 待处理告警 |
| GET | `/alarm/statistics` | 告警统计 |
| GET | `/alarm/export` | 导出告警 |
| GET | `/alarm/dashboard` | 告警仪表盘 |
| POST | `/alarm/disconnect-gateway` | 断开网关 |
| POST | `/alarm/auto-black-screen` | 自动黑屏 |
| DELETE | `/alarm/{id}` | 删除告警 |
| POST | `/alarm/delete-batch` | 批量删除 |

通过 Feign 查询设备、内容、链路上下文，通过 WebSocket 服务推送实时告警。

### 4.4 规则管理

`monitor-platform-rule` 维护检测规则、敏感词和告警阈值。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/rule/detection/page` | 检测规则分页 |
| POST | `/rule/detection/add` | 新增检测规则 |
| POST | `/rule/detection/update` | 更新检测规则 |
| POST | `/rule/detection/delete/{id}` | 删除检测规则 |
| POST | `/rule/detection/status/{id}/{status}` | 启停检测规则 |
| GET | `/rule/detection/list-enabled` | 启用规则列表 |
| POST | `/rule/keyword/page` | 敏感词分页 |
| POST | `/rule/keyword/add` | 新增敏感词 |
| POST | `/rule/keyword/update` | 更新敏感词 |
| POST | `/rule/keyword/delete/{id}` | 删除敏感词 |
| POST | `/rule/keyword/delete-batch` | 批量删除敏感词 |
| POST | `/rule/keyword/status/{id}/{status}` | 启停敏感词 |
| POST | `/rule/keyword/batch-import` | 批量导入敏感词 |
| GET | `/rule/threshold/page` | 阈值配置分页 |
| POST | `/rule/threshold/add` | 新增阈值配置 |
| POST | `/rule/threshold/update` | 更新阈值配置 |
| POST | `/rule/threshold/delete/{id}` | 删除阈值配置 |
| POST | `/rule/threshold/status/{id}/{status}` | 启停阈值配置 |
| GET | `/rule/threshold/list-all` | 全部阈值列表 |
| GET | `/rule/statistics/overview` | 规则统计概览 |

### 4.5 链路管理

`monitor-platform-forward` 负责发布链路配置和网关下发，支持 HTTP 和 MQTT 两种下发模式。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/chain/create` | 创建链路 |
| POST | `/chain/create/basic` | 创建基础链路 |
| POST | `/chain/create/nodes` | 创建链路节点 |
| POST | `/chain/update` | 更新链路 |
| DELETE | `/chain/delete/{chainId}` | 删除链路 |
| POST | `/chain/detail/{chainId}` | 链路详情 |
| GET | `/chain/tree/{chainId}` | 链路树 |
| POST | `/chain/page` | 链路分页 |
| POST | `/chain/validate/{chainId}` | 验证链路 |
| POST | `/chain/validate/structure/{chainId}` | 验证链路结构 |
| POST | `/chain/sync/status/{chainId}` | 同步链路状态 |
| POST | `/chain/sync/all` | 同步所有链路 |
| POST | `/chain/device/{deviceId}` | 设备关联链路 |

链路配置下发支持 MQTT 模式，通过 EMQX Broker 推送到网关。平台统一订阅 `/{tenantId}/{siteId}/{deviceId}/up/#`，设备侧订阅 `/{tenantId}/{siteId}/{deviceId}/down/#`，并保留旧的 `down/proxy-command` 命令 topic 兼容。

MQTT 平台接入点：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/mqtt/auth` | Broker HTTP Auth 回调，校验平台/设备 MQTT 客户端 |
| POST | `/mqtt/webhook` | Broker 生命周期回调，接收设备连接/断开事件 |
| POST | `/tool/mqtt/auth` | `/mqtt/auth` 的兼容路径 |
| POST | `/tool/mqtt/webhook` | `/mqtt/webhook` 的兼容路径 |

### 4.6 UKey 与国密认证

`monitor-platform-Ukey` 提供服务端认证、证书和 UKey 管理能力。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/auth/server/request` | VAuth 服务端认证请求 |
| POST | `/auth/server/verify` | VAuth 认证校验 |
| POST | `/auth/server/error` | 认证错误上报 |
| POST | `/auth/server/key/query` | 密钥查询 |
| POST | `/auth/server/cert/query` | 证书查询 |
| POST | `/cert/register` | 证书注册 |
| POST | `/cert/validate` | 证书校验 |
| GET | `/cert/page` | 证书分页 |
| GET | `/cert/lock-status` | 锁定状态 |
| POST | `/cert/online` | UKey 在线 |
| POST | `/cert/disconnect` | UKey 断开 |
| POST | `/cert/login` | 用户登录 |
| GET | `/cert/check-token` | Token 校验 |
| POST | `/cert/password/change` | 修改口令 |

### 4.7 权限管理

`monitor-platform-role` 负责用户、角色、权限、区域和系统参数。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/role/create` | 创建角色 |
| GET | `/role/get` | 获取角色 |
| POST | `/role/update` | 更新角色 |
| POST | `/role/delete` | 删除角色 |
| GET | `/role/page` | 角色分页 |
| POST | `/role/import` | 导入角色 |
| POST | `/role/permission/replace` | 替换角色权限 |
| POST | `/permission/create` | 创建权限 |
| POST | `/user-role/bind` | 绑定用户角色 |
| GET | `/user-role/permissions` | 用户权限 |
| GET | `/user-role/menu-permissions` | 菜单权限 |
| POST | `/system-parameters/create` | 创建系统参数 |
| GET | `/system-parameters/get` | 获取系统参数 |
| GET | `/system-parameters/getByCode` | 按代码获取参数 |
| POST | `/system-parameters/update` | 更新系统参数 |
| POST | `/system-parameters/delete` | 删除系统参数 |
| GET | `/system-parameters/page` | 参数分页 |

### 4.8 注册服务

系统存在两类注册能力：

| 模块 | 路径 | 说明 |
|------|------|------|
| `monitor-platform-registry-server` | `/registry/*` | 服务实例注册、心跳、发现、统计、客户端配置 |
| `monitor-platform-registry-server` | `/deploy/*` | 网关部署、配置下发、自检 |
| `monitor-platform-device` | `/device/registry/*` | 设备自动注册、心跳、发现 |

`monitor-platform-registry-client` 是 Starter，供 `info-publish-client`、`publish-gateway`、`terminal-gateway` 等组件启动时自动注册。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/registry/register` | 服务注册 |
| DELETE | `/registry/deregister/{instanceId}` | 服务注销 |
| POST | `/registry/heartbeat/{instanceId}` | 心跳上报 |
| GET | `/registry/discover/{serviceName}` | 服务发现 |
| GET | `/registry/instance/{instanceId}` | 实例详情 |
| GET | `/registry/instance/by-client/{clientId}` | 按客户端查询实例 |
| GET | `/registry/services` | 服务列表 |
| GET | `/registry/service-names` | 服务名称列表 |
| PUT | `/registry/status/{instanceId}/{status}` | 更新实例状态 |
| GET | `/registry/statistics` | 注册统计 |
| GET | `/registry/client-config/{clientId}` | 客户端配置 |
| POST | `/registry/client-config/{clientId}` | 更新客户端配置 |
| POST | `/deploy/gateway/register` | 网关注册 |
| GET | `/deploy/gateway/{deviceId}/config` | 网关配置 |
| POST | `/deploy/gateway/{deviceId}/self-test` | 网关自检 |
| POST | `/deploy/logs` | 部署日志 |

### 4.9 日志服务

`monitor-platform-log` 负责链路日志、操作日志、发布日志和诊断事件的记录和查询。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/log/audit/page` | 发布审计聚合日志分页 |
| POST | `/log/link/page` | 链路日志分页 |
| POST | `/log/operation/page` | 操作日志分页 |
| POST | `/log/publish/page` | 发布日志分页 |
| POST | `/log/event/report` | 诊断事件上报 |
| POST | `/log/event/batch-report` | 批量诊断事件上报 |
| POST | `/log/event/page` | 诊断事件分页 |
| POST | `/log/event/timeline` | 诊断事件时间线 |

日志服务通过 `LOG_REPORT_TOKEN` 认证，支持日志去重（`LOG_DEDUP_WINDOW_SECONDS`）和保留天数配置（`LOG_RETENTION_DAYS`）。

## 五、发布客户端

`info-publish-client` 部署在 Windows 侧，源码默认端口 7080，现场配置统一使用 7081，提供来源校验、透明代理、内容预审、安全发布和客户端管理能力。

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| GET | `/api/client/status` | 客户端状态 |
| GET | `/api/client/info-board/status` | 情报板状态 |
| POST | `/api/client/publish/precheck` | 发布预检 |
| POST | `/api/client/publish/verify` | 发布验证 |
| POST | `/api/publish/precheck` | 发布预检（独立接口） |
| POST | `/api/client/commands/execute` | 同步执行控制命令并返回最终结果 |
| GET | `/api/client/commands/{commandTaskId}` | 查询命令状态 |
| GET | `/api/status/overview` | 状态概览 |
| GET | `/api/auth/status` | 认证状态 |
| GET | `/api/config/platform` | 平台配置 |
| PUT | `/api/config/platform` | 更新平台配置 |
| GET | `/api/config/gateway` | 网关配置 |
| POST | `/api/channel/create` | 创建通道 |
| POST | `/api/channel/stop` | 停止通道 |
| GET | `/api/channel/status` | 通道状态 |
| POST | `/api/channel/test` | 测试通道 |
| POST | `/api/ukey/simulate/insert` | 模拟 UKey 插入 |

安全增强能力（默认关闭，需显式开启）：

| 能力 | 接口/配置 | 说明 |
|------|-----------|------|
| 来源校验 | `/security/validate-source` | 发布网关回调校验源 IP、端口、进程 |
| 进程绑定 | `/security/process/*` | 查询候选进程、选择进程、刷新状态 |
| WinDivert 采集 | `/security/windivert/*`、`windivert.*` | UDP 出站流量监听与影子采集 |
| ARP 绑定 | `/api/arp/*` | 将情报板 IP 静态绑定到发布网关 MAC |
| 本地代理 | `/udp-proxy/config`、`transparent-proxy.*` | Windows 侧透明代理到发布网关 relay 端口 |
| 内容预审 | `/security/content-audit/*` | 待发布内容预审、放行或拒绝 |
| 安全发布 | `/security/secure-publish/*` | 安全包扫描、签名、上传、测试 |
| 文件签名记录 | `relay-file-signature.*` | 向发布网关上报签名元数据 |

## 六、发布网关

`publish-gateway/gateway-udp-proxy` 是发布侧核心网关，默认端口 8092，客户端 relay 默认端口 18092。

关键组件：

| 组件 | 职责 |
|------|------|
| `UdpProxyServer` | UDP 代理转发主流程 |
| `TcpProxyServer` / `CatchAllTcpProxyServer` | TCP 转发能力 |
| `UdpProxyRuleManager` | 运行时规则管理 |
| `ClientValidationService` | 调用客户端来源校验 |
| `CryptoService` | 加解密抽象 |
| `VAuthCryptoServiceImpl` | 真实国密加解密，支持 SM4/SVAC 文件加密 |
| `SimpleCryptoServiceImpl` | 开发测试加解密实现 |
| `SignedEnvelopeCryptoService` | 带签名信封的加密能力 |
| `SvacFileCryptoService` | SVAC 文件加密服务 |
| `DataReportService` | 内容解析和上报 |
| `MinioUploadService` | MinIO 对象存储上传 |
| `ProtocolParserRegistry` | 按厂商选择协议解析策略 |
| `SigmaProtocolParser` / `NovaProtocolParser` | Sigma、Nova 协议解析 |
| `SigmaUdpFileAssemblyService` | UDP 文件分包重组 |
| `SecurePublishVerifierService` | 安全发布包验签 |
| `SecurePublishIngressService` | 安全发布入口服务 |
| `SecurePublishJpegSignatureService` | JPEG 签名服务 |
| `RelayFileSignatureService` | 文件签名记录校验 |
| `TrafficReconciliationService` | 客户端与网关流量对账 |
| `AckProxyLearningService` | 回执代理学习、模拟和隔离 |
| `SecureDeliveryService` | 安全投递服务，管理投递任务 |
| `ControlDeliveryService` | 控制命令投递服务 |
| `TranscodeService` | 转码服务，支持透传和 SDK 转码 |
| `ContentReleaseTokenService` | 内容放行令牌服务 |
| `MqttCommandRecordService` | MQTT 命令记录服务 |
| `LocalHttpForwardService` | 本地 HTTP 转发服务 |

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/udp-proxy/config` | 创建/更新代理规则 |
| GET | `/udp-proxy/raw-packets/latest` | 最近原始包 |
| GET | `/udp-proxy/crypto-packets/latest` | 最近加密包 |
| GET | `/udp-proxy/image-payloads/latest` | 最近图片载荷 |
| GET | `/udp-proxy/security/traffic-reconcile` | 流量对账状态 |
| GET | `/udp-proxy/client-relay/status` | 客户端 relay 状态 |
| POST | `/security/relay/file-signature/client-record` | 接收客户端签名记录 |
| POST | `/security/content-token/issue` | 签发内容放行令牌 |
| GET | `/secure-publish/status` | 安全发布状态 |
| POST | `/secure-publish/clear` | 清除安全发布状态 |
| GET | `/secure-publish/ack-proxy/status` | 回执代理状态 |
| GET | `/secure-publish/ack-proxy/samples` | 回执代理样本 |
| GET | `/secure-publish/ack-proxy/mismatches` | 回执不匹配 |
| GET | `/secure-publish/ack-proxy/pending` | 待处理回执 |
| POST | `/secure-publish/ack-proxy/clear` | 清除回执代理 |
| GET | `/assembly/files` | 已重组文件列表 |
| GET | `/assembly/files/{fileId}/download` | 下载重组文件 |
| POST | `/api/crypto/publish-task/encrypt` | 发布任务加密 |
| POST | `/api/crypto/command-task/encrypt` | 命令任务加密 |
| POST | `/api/secure-delivery/tasks` | 创建安全投递任务 |
| GET | `/api/secure-delivery/tasks/{deliveryTaskId}` | 查询投递任务 |
| GET | `/api/secure-delivery/health` | 安全投递健康检查 |
| POST | `/api/secure-delivery/control-tasks` | 创建控制任务 |
| GET | `/api/secure-delivery/control-tasks/{commandTaskId}` | 查询控制任务 |

## 七、终端网关

`terminal-gateway/gateway-udp-proxy` 默认端口 8093，负责终端侧解密和转发。基于 Netty 框架实现高性能网络通信。

关键组件：

| 组件 | 职责 |
|------|------|
| `UdpProxyServer` | 终端侧 UDP 转发 |
| `MessageAssembler` | Message 分片重组 |
| `TcpProxyServer` / `CatchAllTcpProxyServer` | TCP 转发能力 |
| `InfoBoardProbeService` | 情报板连通性探测，支持 TCP/ICMP 探测 |
| `SnapshotSchedulerService` | 屏幕快照调度，支持多策略注册 |
| `ScreenSnapshotStrategyRegistry` | 快照策略注册 |
| `TerminalGatewayMdnsRegistration` | mDNS 服务广播 |

关键接口：

| Method | Path | 说明 |
|--------|------|------|
| POST | `/udp-proxy/config` | 创建/更新终端代理规则 |
| PUT | `/udp-proxy/chain/{chainId}/status` | 更新链路状态 |
| POST | `/udp-proxy/stop/{ruleId}` | 停止规则 |
| GET | `/udp-proxy/status/{ruleId}` | 规则状态 |
| GET | `/udp-proxy/rules/running` | 运行中规则 |
| POST | `/udp-proxy/send-command` | 发送终端命令 |
| GET | `/gateway/auth/status` | 认证状态 |
| POST | `/gateway/auth/reauth` | 重新认证 |

## 八、主要业务流程

### 8.1 链路创建

```text
1. 管控前端创建或导入设备、情报板和链路配置。
2. monitor-device 维护设备台账、位置、状态。
3. monitor-forward 维护链路关系。
4. monitor-forward 或 device 的 gateway-forward-config 接口下发网关规则。
5. publish-gateway / terminal-gateway 加载规则并启动监听。
6. info-publish-client 根据配置执行 ARP 绑定或透明代理。
```

### 8.2 内容发布

```text
1. Sigma 发送 UDP 数据。
2. 数据直接进入 publish-gateway，或经 info-publish-client 透明代理进入 client-relay。
3. publish-gateway 按配置回调 info-publish-client 校验来源。
4. publish-gateway 解析 Sigma/Nova 协议，提取图片、文本、视频或原始文件。
5. 内容上传 MinIO 或以 Base64/引用方式上报 monitor-content。
6. publish-gateway 加密并按 Message 协议封装后发送 terminal-gateway。
7. terminal-gateway 重组、解密，并转发给情报板。
8. 情报板响应按反向链路加密回传给发布端。
```

### 8.3 内容检测与告警

```text
1. monitor-content 接收发布网关内容、终端快照或文件引用。
2. monitor-content 调用规则服务获取启用规则、敏感词和阈值。
3. 本地规则或 AI 检测判定异常。
4. monitor-content 生成告警并调用 monitor-alarm。
5. monitor-alarm 记录待处理告警、统计和处置结果。
6. monitor-websocket 将实时事件推送到前端。
```

### 8.4 设备注册与保活

```text
1. 各客户端和网关启动后使用 registry-client 自动注册。
2. 公网 MQTT 场景下，网关启动后主动连接 EMQX，并通过 `up/register` 上报 serviceName、instanceId、IP、端口、MAC、deviceType、版本和厂商型号。
3. 注册信息落到 monitor-device 的 `/device/registry/auto-register`，复用 `unified_device_info` 设备台账；云端 Nacos 不可用或禁用服务发现时，monitor-forward 必须配置 `monitor.device.url` 直连 monitor-device。
4. 设备定时通过 `up/heartbeat` 上报心跳；Broker Webhook 可辅助标记连接/断开状态。
5. terminal-gateway 定时探测情报板并调用 `/device/unified/batch-status`。
6. monitor-device 汇总注册、心跳与探测结果，更新在线/离线状态。
7. 离线或异常达到阈值后触发告警。
```

## 九、安全体系

| 层级 | 机制 | 实现点 |
|------|------|--------|
| 网络路径控制 | ARP 绑定、透明代理、client-relay | `info-publish-client`、`publish-gateway` |
| 来源鉴权 | IP、端口、进程绑定 | `/security/validate-source`、`ClientValidationService` |
| 内容预审 | 发布前检测、内容放行令牌 | `content-pre-audit`、`ContentReleaseTokenService` |
| 文件完整性 | 客户端签名记录、网关验签 | `RelayFileSignatureService`、`SecurePublishVerifierService` |
| 传输加密 | VAuth/SM4、签名信封、SVAC 文件加密 | `VAuthCryptoServiceImpl`、`SignedEnvelopeCryptoService`、`SvacFileCryptoService` |
| 安全投递 | 任务投递、控制命令加密 | `SecureDeliveryService`、`ControlDeliveryService` |
| 流量对账 | 客户端与网关窗口内字节对账 | `TrafficReconciliationService` |
| 终端状态 | 心跳、TCP/ICMP 探测、屏幕快照 | `InfoBoardProbeService`、`SnapshotSchedulerService` |
| MQTT 通信 | EMQX Broker、HTTP Auth、Webhook、命令记录、主动注册 | `MqttCommandRecordService`、`MqttAccessController`、`MqttRegisterHandler`、`LocalHttpForwardService` |

## 十、部署注意事项

| 项 | 当前状态 | 建议 |
|----|----------|------|
| `monitor-platform` 与 `monitor-platform-new` 并存 | 本文只对齐 `monitor-platform` | 后续文档避免混用两个目录 |
| `monitor-platform-monolith` | 存在单体模块，另有 `deploy/monolith-host/docker-compose.yml` | 若采用微服务部署，不应同时暴露单体同名接口 |
| `monitor-content` 网络模式 | docker-compose 使用 `network_mode: host` | 依赖地址必须通过 `CONTENT_*` 环境变量明确覆盖 |
| MySQL 版本 | POM 驱动为 8.0.33/8.4.0，部署镜像为 MySQL 5.7 | SQL 迁移前需要验证 5.7/8.0 兼容性 |
| 配置注释编码 | 多个 yml 注释存在乱码 | 不影响键值读取，但建议统一 UTF-8 重新保存注释 |
| UKey/国密库 | docker-compose 挂载 `/opt/vauth/lib` 和 USB 设备 | 生产环境需确认硬件、PIN、证书序列号和 `LD_LIBRARY_PATH` |
| EMQX MQTT Broker | 已部署 EMQX 5.4.0，支持 MQTT v3.1.1/v5.0 | 需配置认证、ACL 和 TLS 证书 |
| 链路下发模式 | 支持 HTTP 和 MQTT 两种模式 | 通过 `DISPATCH_MODE` 环境变量配置，MQTT 模式需配置 Broker 连接 |
| 日志服务 | 新增 `monitor-platform-log` 模块 | 支持链路日志、操作日志、发布日志和诊断事件，通过 `LOG_REPORT_TOKEN` 认证 |

## 十一、验证方式

```powershell
# 1. 确认 monitor-platform 模块
Get-Content D:\project02\monitor-platform\pom.xml -Raw

# 2. 确认部署端口
Get-Content D:\project02\monitor-platform\deploy\docker-compose.yml -Raw

# 3. 校验接口仍存在
rg -n "@RequestMapping|@GetMapping|@PostMapping" D:\project02\monitor-platform D:\project02\info-publish-client D:\project02\publish-gateway D:\project02\terminal-gateway -g "*.java"

# 4. 检查日志服务模块
rg -n "@RequestMapping" D:\project02\monitor-platform\monitor-platform-log -g "*.java"

# 5. 检查 MQTT 相关配置
rg -n "mqtt|emqx" D:\project02\monitor-platform\deploy -g "*.yml" -g "*.yaml"

# 6. 编译验证，需 Maven 和依赖仓库可用
cd D:\project02\monitor-platform; mvn -DskipTests package
cd D:\project02\info-publish-client; mvn -DskipTests package
cd D:\project02\publish-gateway; mvn -DskipTests package
cd D:\project02\terminal-gateway; mvn -DskipTests package
```
