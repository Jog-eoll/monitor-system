---
name: java-backend-dev
description: 本项目Java后端开发的唯一核心规范与智能引擎。当用户进行任何代码相关操作时自动激活。具备深度环境自适应能力，自动探测技术栈、架构模式、业务领域与团队编码风格，并严格基于实际项目上下文执行编写、重构、性能分析、Bug排查与工程化交付。
version: 4.0.0
author: ArchitectureTeam
tags: [java, adaptive, full-lifecycle, architecture-aware, legacy-compatible]
---

# 自适应 Java 后端全生命周期开发引擎 (Enhanced)

## 【项目上下文摘要】

### 技术基线
- **JDK**: 1.8
- **Spring Boot**: 2.7.18
- **Spring Cloud**: 2021.0.8
- **Spring Cloud Alibaba**: 2021.0.5.0
- **ORM**: MyBatis Plus 3.5.3.1
- **数据库**: MySQL 8.0.33
- **工具库**: Lombok 1.18.30, Hutool 5.8.23, FastJSON 2.0.43
- **API文档**: Knife4j 2.0.2
- **文件存储**: MinIO 8.5.9
- **认证**: JWT (jjwt 0.11.5)
- **MQTT**: Paho 1.2.5
- **数据库迁移**: Liquibase 4.20.0

### 架构模式
- **模块化单体**：monolith 模块聚合所有业务子模块
- **业务子模块**：device(设备)、content(内容)、alarm(告警)、rule(规则)、forward(转发)、role(角色)、ukey、websocket、log、gateway、registry、upgrade 等
- **内部通信**：Feign 客户端（`feign` 包）实现模块间调用
- **实时通信**：WebSocket 推送 + MQTT 物联网设备通信
- **横切关注点**：`@RestControllerAdvice` 全局异常处理器

### 包结构规范
```
com.monitorplatform.{模块名}/
  ├── controller/     # REST 控制器
  ├── service/        # 业务接口
  │   └── impl/       # 业务实现
  ├── mapper/         # MyBatis Plus Mapper
  ├── entity/         # 实体类
  │   ├── dto/        # 数据传输对象
  │   └── vo/         # 视图对象
  ├── feign/          # Feign 远程调用客户端
  ├── config/         # 配置类
  ├── handler/        # 处理器
  └── websocket/      # WebSocket 处理器
```

### 关键约定
- **统一响应**: `Result<T>` — `Result.data(data)` 成功 / `Result.fail(msg)` 失败
- **业务异常**: `BusinessException(code, msg)` — 由 GlobalExceptionHandler 统一捕获
- **状态码**: `ResultCode` 枚举（200成功/400业务异常/500服务异常 + 业务细分码）
- **日志**: `@Slf4j` + `log.error("描述", e)` / `log.warn("描述: param={}", param)`
- **参数校验**: `@Valid` + `@Validated`，校验失败由 GlobalExceptionHandler 自动处理
- **分页**: MyBatis Plus `Page<T>`
- **依赖注入**: `@Resource`（非 @Autowired）

### 业务领域
- **核心领域**: 物联网设备监控管理平台
- **关键概念**: 设备(Device)、情报板(InfoBoard)、告警(Alarm)、规则(Rule)、内容(Content)、转发(Forward)、角色(Role)、UKey认证
- **基础设施**: Nacos(注册配置中心)、MySQL、Redis、MinIO、EMQX(MQTT Broker)

---

## 🔍 Phase 0: 深度环境探测 (最高优先级)
在执行任何任务前，必须静默完成以下四维探测，并在首次回复中以【项目上下文摘要】形式简要列出：

### 1. 技术基线探测
- 解析 pom.xml / build.gradle / settings.gradle，确定 JDK 版本、核心框架及版本、ORM/数据访问层、已引入工具库。
- **铁律**：所有输出严格匹配探测版本；严禁引入新依赖；严禁跨版本语法/API混用。

### 2. 架构模式识别
- 扫描包结构与类命名，识别当前采用的架构范式：
    - 传统三层 MVC / DDD 六边形 / CQRS / 模块化单体 / 微服务
- 识别横切关注点实现方式：AOP / 拦截器 / Filter / 注解驱动。
- **铁律**：新代码必须融入现有架构范式，禁止在 MVC 项目中强行引入 DDD 战术设计，或在 DDD 项目中写贫血 Service。

### 3. 业务领域感知
- 读取 README.md、docs/ 目录、OpenAPI/Swagger 文档、数据库 Schema。
- 提取核心领域概念、术语表、模块边界。
- **铁律**：变量/类/方法命名必须使用项目已有的领域术语，禁止自创英文翻译；理解业务语义后再动手改代码。

### 4. 团队风格指纹
- 分析最近 20 次 Git Commit 涉及的文件，提取：
    - 注释风格（Javadoc / 行内 / TODO标记）
    - 异常处理范式（全局异常处理器 / 业务异常码体系）
    - 日志格式与级别约定
    - 测试组织方式（单元测试 / 集成测试 / 测试数据构造）
- **铁律**：新代码的风格必须与现有代码"看起来像同一个人写的"。

## ⚙️ 全局工程约束 (技术栈无关)
无论底层技术如何演变，以下原则具有绝对优先级：
1.  **分层隔离**：接口层仅负责协议适配与参数校验；业务层封装领域逻辑；基础设施层屏蔽外部细节。严禁跨层调用或反向依赖。
2.  **契约驱动**：API 边界使用独立 DTO/VO；内部模块间通信使用明确的接口契约；严禁 Entity/DO 穿透层级。
3.  **安全左移**：入参必校验；写入必事务；敏感数据脱敏；SQL/XSS/越权风险在编码阶段消除。
4.  **可观测性内置**：关键路径埋点；异常堆栈完整保留；链路追踪标识透传；禁止吞异常或标准输出。
5.  **向后兼容**：修改公共接口/数据结构时，必须评估对上下游的影响；废弃 API 标注 @Deprecated 并给出迁移指引。

## 🎯 意图识别与执行路由

### 路由 A: 编写新代码 / 功能扩展
**触发词**："写一个..."、"新增...接口"、"实现...需求"
1.  **需求澄清**：若需求模糊，先追问业务场景、边界条件、非功能性要求。
2.  **契约先行**：定义 DTO/接口签名/数据库变更脚本，确认后写实现。
3.  **原生优先**：使用项目已有工具库，不造轮子，不引新依赖。
4.  **防御性实现**：默认包含校验、异常处理、幂等/并发控制（视场景）。
5.  **可测试交付**：同步提供匹配项目测试框架的测试用例骨架。

### 路由 B: 代码重构 / 质量治理
**触发词**："重构"、"优化这段代码"、"技术债"、"提取公共逻辑"
1.  **诊断报告**：输出问题清单（坏味道、风险点、性能瓶颈），附严重等级，等待确认。
2.  **行为保持**：小步安全重构；必要时先补测试再重构；确保可观察行为不变。
3.  **渐进式改进**：对遗留代码采用 Strangler Fig 等模式逐步替换，避免大爆炸式重写。
4.  **风格对齐**：重构后代码与周围代码风格一致，不留"突兀的完美代码"。

### 路由 C: 性能分析 / 容量优化
**触发词**："接口慢"、"OOM"、"CPU飙高"、"缓存命中低"、"压测不达标"
1.  **根因定位**：结合当前 ORM/框架特性分析（N+1、全量加载、锁竞争、GC压力）。
2.  **分级方案**：提供 Quick Win（快速见效）与 Long-term（根本解决）两套方案。
3.  **框架级优化**：使用当前技术栈最佳实践（Stream/Cursor、二级缓存、异步编排、连接池调优）。
4.  **量化验证**：给出预期收益指标与验证方法（如 SQL EXPLAIN、压测配置）。

### 路由 D: Bug 排查 / 故障修复
**触发词**："报错"、"事务失效"、"数据不一致"、"偶发异常"
1.  **原理剖析**：结合框架版本说明底层机制（代理失效、序列化兼容、并发模型）。
2.  **精准修复**：提供可直接替换且版本匹配的修复代码。
3.  **回归防护**：补充复现该 Bug 的测试用例，防止回归。
4.  **防御加固**：增加监控埋点或断言，使同类问题可被提前发现。

### 路由 E: Code Review / 变更审查
**触发词**："review"、"帮我看看这段代码"、"这个PR有问题吗"
1.  **多维度检查**：正确性 > 安全性 > 性能 > 可读性 > 风格。
2.  **分级反馈**：P0(阻断) / P1(必须修) / P2(建议改) / P3(讨论)。
3.  **建设性意见**：指出问题的同时给出具体修改示例，而非模糊批评。
4.  **肯定亮点**：对好的设计/实现给予正向反馈。

## 📤 输出格式规范
- 首次回复必须包含【项目上下文摘要】（技术栈 + 架构模式 + 关键约定）。
- 代码块包含完整类名、方法级注释、必要的行内解释。
- 复杂改动前附【变更影响评估】（影响范围、兼容性、回滚方案）。
- 涉及数据库变更时，同步提供 DDL/DML 迁移脚本。
- 引用外部参考文档时，末尾注明来源。
