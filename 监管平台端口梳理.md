# 监管平台端口梳理

> 适用范围：`monitor-platform`、`publish-gateway`、`terminal-gateway`、`info-publish-client` 及监管服务器公共组件。
>


## 一、端口总览

| 序号 | 项目 | 最终建议端口 | 协议 | 说明 |
| --- | --- | --- | --- | --- |
| 1 | 监管平台 Web/API 对外端口 | `80` / `443` | TCP | Nginx 统一入口；HTTP `80`，启用 TLS 时 `443`。 |
| 2 | 监管平台 API 网关端口 | `8060` | TCP | 微服务模式下 `monitor-gateway` 对外/对内 API 网关端口。 |
| 3 | 监管平台单体后端端口 | `8080` | TCP | 融合/单体部署时平台 HTTP 统一端口，即 `PLATFORM_HTTP_PORT`。 |
| 4 | 监管平台后端服务端口 | `8062` - `8071` | TCP | 微服务拆分端口，详见下一节。 |
| 5 | 监管服务器 Nginx 端口 | `80` / `443` | TCP | `monitor-nginx` 监听端口；反代到 `monitor-gateway:8060` 或单体 `8080`。 |
| 6 | 内容识别 API 端口 | `8065` / `8080` / `21580` | TCP | 平台内容服务微服务端口为 `8065`；单体模式为 `8080`；本地 AI 审核模型默认端口为 `21580`。 |
| 7 | 内容识别服务 Nginx 端口 | 未单独定义 | TCP | 当前仓库未见独立内容识别 Nginx；通常走平台 Nginx `80/443` 或直接访问内容 API。 |
| 8 | 监管服务器访问内容识别服务器端口 | `8065` / `8080` / `21580` | TCP | 根据部署形态选择：微服务内容模块、单体平台或独立 AI 审核服务；平台内部通过 `LOCAL_AUDIT_*` 指向本地模型。 |
| 9 | 内容识别服务器回调监管服务器 | 有 | TCP | 内容服务会按配置访问规则、告警、设备、转发等平台服务；微服务模式涉及 `8062`、`8064`、`8066`、`8067`。 |
| 10 | 加密/发布网关 HTTP 端口 | `8092` | TCP | `publish-gateway` Spring Boot 入口，承载链路配置、安全投递、控制任务、健康检查和诊断接口。 |
| 11 | 加密/发布网关 UDP 中继端口 | `18092` | UDP | `client-relay.relay-port` 默认端口，用于信发客户端到发布网关的 UDP 中继；关闭 `CLIENT_RELAY_ENABLED` 时可不开放。 |
| 12 | 解密/终端网关 HTTP 端口 | `8093` | TCP | `terminal-gateway` Spring Boot 入口，承载链路配置、解密投递、设备注册、协议元数据和健康检查。 |
| 13 | 解密/终端网关设备侧默认端口 | `9520` | UDP/TCP | 青松/JetFileII 和固定屏幕配置默认端口；实际情报板端口以设备协议和链路规则为准。 |
| 14 | MySQL | `23306` | TCP | 公共组件最终端口；容器内部仍为 `3306`。 |
| 15 | Redis | `26379` | TCP | 公共组件最终端口；容器内部仍为 `6379`。 |
| 16 | MinIO API | `29000` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `19000`，需要同步调整。 |
| 17 | MinIO Console | `29001` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `19002`，需要同步调整。 |
| 18 | Nacos HTTP | `28848` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `18848`，需要同步调整。 |
| 19 | Nacos gRPC | `29848` | TCP | Nacos 2.x gRPC 端口；仓库旧默认值为 `19848`，需要同步调整。 |
| 20 | Nacos Raft | `29849` | TCP | Nacos 2.x Raft/集群端口；仓库旧默认值为 `19849`，需要同步调整。 |
| 21 | RabbitMQ | 待确认 | TCP | 当前仓库仅见 AMQP 依赖，未见 RabbitMQ 部署配置或固定端口。若现场启用，需要由部署配置确认。 |
| 22 | SSH 运维端口 | `22` | TCP | 如现场改过 SSH 端口，以服务器实际配置为准。 |
| 23 | EMQX MQTT Broker | `1883` / `8883` / `18083` | TCP | MQTT TCP、MQTT TLS、EMQX Dashboard。公网 MQTT 场景至少放行 `1883` 或 `8883` 给设备主动连接。 |

运行态备注：2026-07-01 已将 `192.168.1.25` 现场微服务部署的业务库与 Nacos 配置库连接切到 `192.168.1.31:3306`；`23306` 仅表示本机 MySQL 容器暴露端口，当前业务服务不再以它作为数据库连接目标。

## 二、监管平台微服务端口

| 服务 | 端口 | 用途 |
| --- | --- | --- |
| `monitor-gateway` | `8060` | API 网关、统一路由、Nginx 上游。 |
| `monitor-device` | `8062` | 设备、统一设备、注册数据落库。 |
| `monitor-ukey` | `8063` | UKey、证书校验、上线下线、心跳。 |
| `monitor-alarm` | `8064` | 告警服务。 |
| `monitor-content` | `8065` | 内容接收、内容识别、预审核接口。 |
| `monitor-rule` | `8066` | 规则服务。 |
| `monitor-forward` | `8067` | 链路编排、网关/客户端配置下发。 |
| `monitor-role` | `8068` | 角色权限。 |
| `monitor-registry-server` | `8069` | 注册中心业务服务、客户端注册/配置。 |
| `monitor-websocket` | `8070` | WebSocket 推送。 |
| `monitor-log` | `8071` | 诊断日志上报、查询。 |

## 三、加密/解密网关端口

| 网关/组件 | 端口 | 协议 | 配置来源 | 用途与放行口径 |
| --- | --- | --- | --- | --- |
| 加密/发布网关 HTTP 入口 | `8092` | TCP | `publish-gateway/gateway-udp-proxy/src/main/resources/application.yml` 的 `server.port`；Dockerfile `EXPOSE 8092` | 平台内网直连、信发客户端安全投递、发布网关健康检查、`/udp-proxy/**`、`/api/secure-delivery/**`、`/api/crypto/**`、`/security/**` 等接口统一复用该端口。 |
| 加密/发布网关 UDP 中继 | `18092` | UDP | `client-relay.relay-port` / `CLIENT_RELAY_PORT`，默认 `18092` | 信发客户端到发布网关的 UDP 中继入口；如禁用 `CLIENT_RELAY_ENABLED`，该端口不作为必开项。 |
| 加密/发布网关透明代理监听 | 按链路规则 | UDP/TCP | 平台下发 `/udp-proxy/config` 后由规则中的 `listenIp`、`listenPort` 决定 | 可能复用情报板业务端口，如 `9520`；只有部署透明代理规则且本机需要绑定对应 IP/端口时才放行。 |
| 解密/终端网关 HTTP 入口 | `8093` | TCP | `terminal-gateway/gateway-udp-proxy/src/main/resources/application.yml` 的 `server.port`；Dockerfile `EXPOSE 8093` | 接收平台或发布网关配置，承载 `/udp-proxy/**`、`/api/secure-command/**`、`/api/devices/**`、`/api/device-protocol/**` 和 `/api/command/**`。 |
| 解密/终端网关设备侧通信 | `9520` 或设备实际端口 | UDP/TCP | `FIXED_SCREEN_0_PORT` 默认 `9520`；协议默认端口也按厂商能力决定 | 终端网关到情报板/LED 终端的出方向业务端口；青松/JetFileII 常用 `9520`，Nova/卡莱特等按实际协议和设备配置确认。 |
| 网关 mDNS 自动发现 | `5353` | UDP | `provider.jmdns.*` / `PROVIDER_JMDNS_ENABLED` | 发布网关和终端网关启用同网段自动发现时需要放行；关闭 JmDNS 时不作为必开项。 |
| 网关 MQTT 主动连接 | `1883` 或 `8883` | TCP | `MQTT_BROKER_URL` | 发布网关、终端网关作为 MQTT 客户端主动连 EMQX；Broker 侧开放 `1883/8883`，网关侧按出站放行，不新增网关入站端口。 |
| 发布网关本机 MySQL/Redis | `3307` / `6379` | TCP | `publish-gateway/deploy/docker-compose.yml` 绑定 `127.0.0.1:${MYSQL_HOST_PORT:-3307}`、`127.0.0.1:${REDIS_HOST_PORT:-6379}` | 仅发布网关宿主机本机回环访问，不作为跨主机防火墙放行口径。 |
| 终端网关本机 MySQL | `3306` | TCP | `terminal-gateway/deploy-package/docker-compose.yml` 使用 host 网络，默认 `DB_PORT=3306` | 仅终端网关宿主机本机数据库访问；如改为外部数据库，以现场外部数据库端口为准。 |

## 四、网关与监管平台交互端口

| 方向 | 端口 | 主要接口/用途 | 备注 |
| --- | --- | --- | --- |
| 监管平台 -> 发布网关 | `8092` | `POST /udp-proxy/config`、`PUT/DELETE /udp-proxy/chain/**`、`/api/secure-delivery/**` | 平台下发发布链路配置、查询任务、触发安全投递或控制投递；MQTT 场景下也由发布网关本机 HTTP 转发复用该端口。 |
| 监管平台 -> 终端网关 | `8093` | `POST /udp-proxy/config`、`/api/secure-command/**`、`/api/devices/**`、`/api/device-protocol/**` | 内网 HTTP 直连场景下发终端网关监听和转发配置；发布网关安全投递到解密网关也复用该端口。 |
| 发布网关 -> 监管平台 UKey 服务 | `8063` 或 `8080` | `/cert/validate`、`/cert/online`、`/cert/heartbeat` | 微服务走 `8063`；单体走 `8080`。 |
| 发布网关 -> 监管平台内容服务 | `8065` 或 `8080` | 内容上报、内容检测、预审核；`SECURE_PUBLISH` 复用 `/content/receive` 写入 `contentType=playlist` 发布清单记录，终端投递成功后复用 `/content/detection/detect` 写入 `image`/`video` 检测记录 | 微服务走 `8065`；单体走 `8080`。 |
| 发布网关 -> 监管平台设备服务 | `8062` 或 `8080` | 设备注册、设备状态 | 微服务走 `8062`；单体走 `8080`。 |
| 发布网关 -> 监管平台日志服务 | `8071` 或 `8080` | 诊断日志上报 | 微服务走 `8071`；单体走 `8080`。 |
| 发布网关 -> EMQX Broker | `1883` 或 `8883` | MQTT 注册、心跳、回执、下行命令 | 设备主动连接公网 Broker，平台不直接访问设备内网 IP。 |
| 终端网关 -> 监管平台 UKey/设备/内容服务 | `8063/8062/8065` 或 `8080` | 证书校验、状态上报、截图回传 | 微服务标准模板默认走 `8063/8062/8065`；单体/融合模板单独保留 `8080`。 |
| 终端网关 -> EMQX Broker | `1883` 或 `8883` | MQTT 注册、心跳、回执、下行命令 | 与发布网关一致由 `mqtt-agent.*` 配置控制；微服务模板默认启用 MQTT Agent，不新增终端网关入站端口。 |
| 终端网关 -> 情报板/LED 终端 | `9520` 或设备实际端口 | 解密后 UDP/TCP 投递、设备探测、屏幕命令 | `FIXED_SCREEN_0_PORT` 和青松/JetFileII 默认端口为 `9520`；其他厂商端口按协议和现场设备配置确认。 |
| mDNS 自动发现 | `5353` | UDP | 如果启用自动发现，需要同网段放行 `5353/udp`。 |

## 五、信发客户端与监管平台交互端口

| 方向 | 端口 | 主要接口/用途 | 备注 |
| --- | --- | --- | --- |
| `info-publish-client` 对外 HTTP | `7081` | 接收平台/发布网关配置下发 | 现场配置和安装日志均使用 `7081`；源码默认 `7080`。 |
| 监管平台 -> `info-publish-client` | `7081` | `POST /udp-proxy/config` | 下发发布网关、终端网关、情报板、MAC 等链路配置。 |
| 监管平台 -> `info-publish-client` | `7081` | `PUT /udp-proxy/chain/{chainId}/status`、`DELETE /udp-proxy/chain/{chainId}` | 链路启停和删除。 |
| 发布网关 -> `info-publish-client` | `7081` | 中转 `POST /udp-proxy/config`，按需查询 `/security/traffic/stats` 做流量对账 | `publish-gateway/deploy/application.yml` 已是 `CLIENT_PORT=7081`；源码默认仍有 `7080`，部署需覆盖；发布网关不再逐包回调 `/security/validate-source`。 |
| 发布网关 -> `info-publish-client` | `7081` | `GET /api/client/publish/v2/files/{token}` | 仅 V2 multipart 内容发布使用；现场客户端可达地址为 `192.168.1.168:7081`，客户端生成临时文件下载地址后，发布网关仍按既有 `files[].fileUrl` 拉取，不新增端口。 |
| `info-publish-client` -> 监管平台注册服务 | `8080` 或 `8069` | `POST /registry/register`、`GET /registry/client-config/{clientId}` | 现场配置示例为 `192.168.1.233:8080`；微服务直连可走 `8069` 或平台统一入口。 |
| `info-publish-client` -> 监管平台 UKey 服务 | `8080` 或 `8063` | `/cert/validate`、`/cert/online`、`/cert/heartbeat`、`/api/client/ukey/register` | 由远程配置中的 `monitorPlatformUrl` 或本地 `monitor-platform.url` 决定。 |
| `info-publish-client` -> MySQL | `23306` 或现场外部库端口 | 注册配置 HTTP 失败时兜底读取 `registry_client_config` | 当前 `192.168.1.25` 现场配置指向 `192.168.1.31:3306`；通用部署可继续按公共组件口径使用 `23306`。 |
| `info-publish-client` -> 内容预审核 | `8065` / `8080` | `/content/detection/pre-audit` | 客户端仍调用平台内容服务；平台内容服务再按 `LOCAL_AUDIT_*` 配置访问 `21580` 本地审核模型。 |

## 六、信发客户端与网关/信发板端口

| 方向 | 端口 | 协议 | 用途 |
| --- | --- | --- | --- |
| `info-publish-client` -> 发布网关 | `8092` | TCP | 安全投递、发布网关健康检查、转发控制；加密网关 HTTP 入口统一复用该端口。 |
| 发布网关 UDP 中继 | `18092` | UDP | `info-publish-client` 到发布网关/透明转发链路的客户端 UDP 中继端口。 |
| 信发板常见业务端口 | `9520` | UDP/TCP | 默认目标端口，具体以设备配置为准。 |
| Nova/厂商协议端口 | `16606`、`16602`、`16601`、`16611`、`19999` | UDP/TCP | 源码和运行日志中出现的厂商/设备端口，按现场设备协议确认。 |

## 七、防火墙放行建议

| 服务器/主机 | 入方向建议放行 | 出方向建议放行 |
| --- | --- | --- |
| 监管服务器 | `80/tcp`、`443/tcp`、`8060-8071/tcp`、`8080/tcp`、`1883/tcp` 或 `8883/tcp`、`18083/tcp`、`23306/tcp`、`26379/tcp`、`28848/tcp`、`29848/tcp`、`29849/tcp`、`29000/tcp`、`29001/tcp`、`22/tcp` | 到发布网关 `8092/tcp`、终端网关 `8093/tcp`、信发客户端 `7081/tcp`、平台内容识别服务 `8065/8080/tcp`；若本地审核模型独立部署，还需平台内容服务访问 `21580/tcp`。公网 MQTT 场景由设备主动连入 EMQX。 |
| 发布网关服务器 | `8092/tcp`、`18092/udp`、启用 mDNS 时 `5353/udp`、透明代理规则实际监听端口 | 到监管平台 `8062/8063/8065/8071/tcp` 或单体 `8080/tcp`，到 EMQX `1883/8883/tcp`，到信发客户端 `7081/tcp`，到终端网关 `8093/tcp` 和设备业务端口；本机 MySQL/Redis 默认 `127.0.0.1:3307/6379`，不对外放行。启用 SVAC USB 模组门禁时，本机需容器只读访问 `/sys/bus/usb`，不新增网络端口。 |
| 终端网关服务器 | `8093/tcp`、启用 mDNS 时 `5353/udp`、终端侧实际监听/业务 UDP/TCP 端口 | 到监管平台 `8080/tcp` 或 `8062/8063/8065/tcp`，到 EMQX `1883/8883/tcp`，到 MinIO `29000/tcp`，到情报板/LED 终端 `9520/tcp/udp` 或设备实际端口；本机 MySQL 默认 `127.0.0.1:3306`，不对外放行。启用 SVAC USB 模组门禁时，本机需容器只读访问 `/sys/bus/usb`，不新增网络端口。 |
| 信发客户端主机 | `7081/tcp`、`18092/udp`、信发板业务端口 | 到监管平台 `8080/8069/8063/8065/tcp`，到发布网关 `8092/tcp`，到 MySQL `23306/tcp`。 |

## 八、需要同步调整或确认的项

1. `MinIO`、`Nacos` 在 `monitor-platform/deploy/stage2` 中仍保留 `19000/19002`、`18848/19848/19849` 旧默认值；若按公共组件前缀 `2` 的最终口径部署，需要同步改为 `29000/29001`、`28848/29848/29849`。
2. `info-publish-client` 源码默认 `server.port=7080`，但现场配置、安装日志和发布网关部署配置应统一为 `7081`。
3. `publish-gateway/gateway-udp-proxy/src/main/resources/application.yml` 中 `CLIENT_PORT` 默认仍是 `7080`；实际部署应使用 `publish-gateway/deploy/application.yml` 或环境变量覆盖为 `7081`。
4. `info-publish-client` 内容发布链路不新增端口，但需要按现场网络配置超时：`HTTP_CLIENT_CONNECT_TIMEOUT_MS` 默认 `10000`，`HTTP_CLIENT_READ_TIMEOUT_MS` 默认 `120000`，`CONTENT_PUBLISH_DOWNLOAD_TIMEOUT_MS` 默认 `120000`；青松、V2 对接方或前端调用 `7081` 内容发布接口时，HTTP client 超时不应低于业务等待时间。V2 multipart 内容发布要求发布网关可访问 `content-publish.v2.file-download-base-url` 指向的客户端地址，现场默认 `http://192.168.1.168:7081`；客户端 multipart 总请求按 `2300MB`、单文件按 `2048MB` 配置，临时文件 TTL 为 `900000ms` 且从落盘完成后计算。
5. 发布网关 V2 内容下载仍复用 `8092/tcp` 的 `/api/secure-delivery/tasks`，不新增端口；为支持客户端 2GB multipart 文件，发布网关 `SECURE_DELIVERY_DOWNLOAD_MAX_FILE_SIZE_MB` 默认调整为 `2048`，`SECURE_DELIVERY_DOWNLOAD_TIMEOUT_MS` 默认调整为 `900000`。视频大文件默认通过 `SECURE_DELIVERY_LARGE_FILE_REF_ENABLED=true` 走 file-ref 清单，发布网关不再把视频转成 `contentBase64` 巨型 JSON；终端网关收到 `/api/secure-command/publish` 后按文件引用下载并流式上传，下载限制由 `SECURE_COMMAND_FILE_REF_MAX_FILE_SIZE_MB`、`SECURE_COMMAND_FILE_REF_DOWNLOAD_TIMEOUT_MS` 和 `SECURE_COMMAND_FILE_REF_TEMP_DIR` 控制，网络仍复用既有 8092/8093 和文件服务访问方向。JetFileII 设备侧普通 `WRITE_PATHFILE` 仍受 65535 包限制，超限且文件小于 2GB 时终端网关自动切换 `WRITE_PATHFILE_EXT`。已验证设备可显式设置 `SECURE_COMMAND_FILE_UPLOAD_LARGE_VIDEO_MODE=WRITE_BIGFILE_CRC_TCP` 使用 `0x0215`，仍复用设备实际端口（青松默认 `9520/tcp`），不新增网关监听端口；默认保持 `DISABLED`，`QS0925` 不支持。`SECURE_PUBLISH` 投递成功后的媒体检测也只访问监管平台内容服务既有 `8065/8080`，不新增网关或平台端口。
6. `RabbitMQ` 当前仓库没有部署文件和连接配置，不能直接作为必开端口；如果现场确实启用，需要补充 broker 地址、AMQP 端口、管理端口和访问方向。
7. SSH 默认为 `22/tcp`，如果服务器安全基线改过端口，应以 `/etc/ssh/sshd_config` 和防火墙实际规则为准。
8. Nginx 微服务入口统一识别后端服务前缀并转发到 `monitor-gateway:8060`，支持无 `/api` 的业务路径（如 `/content/**`、`/log/**`、`/upgrade/**`），同时兼容 `/api/**` 并在转发前剥离 `/api` 前缀；具体服务归属仍由网关路由决定。
9. MQTT Broker Auth/Webhook 使用平台现有 HTTP 端口承载：微服务走 `monitor-forward:8067`，单体走 `8080`，不新增独立平台 HTTP 端口。
   - MQTT 业务命令接口 `/api/mqtt/commands/query-status`、`/api/mqtt/commands/control-delivery`、`/api/mqtt/commands/{messageId}` 同样复用 `monitor-forward:8067` 或单体 `8080`，发布网关和终端网关侧 `QUERY_STATUS` / `NOOP` / `ECHO` / `CONTROL_DELIVERY` 通过既有 EMQX `1883/8883` 通道回执，不新增网关入站端口。
   - 发布网关 `CONTROL_DELIVERY` 最终态轮询只调用本机 `ControlDeliveryService` 状态接口，不新增网络端口；可通过 `CONTROL_DELIVERY_MQTT_FINAL_REPLY_POLL_INTERVAL_MS` 和 `CONTROL_DELIVERY_MQTT_FINAL_REPLY_TIMEOUT_MS` 调整轮询间隔和超时。
   - 发布网关 MQTT action 的 HTTP 转发仍复用既有目标端口，不新增端口；生产默认 `MQTT_HTTP_FORWARD_TIMEOUT_MS=15000`，用于覆盖 `/udp-proxy/config` 自应用后继续中转客户端配置的耗时。
   - 终端网关固定屏幕清单启动注册调用本机 `127.0.0.1:8093/api/devices/register`，复用现有 `8093/tcp`，不新增入站端口；部署包默认可配置 `FIXED_SCREEN_0_IP=192.168.113.88`、`FIXED_SCREEN_0_PORT=9520`、`FIXED_SCREEN_0_VENDOR=QINGSONG`。
10. 云端 MQTT 最小部署如禁用 Nacos 服务发现，`monitor-forward` 仍需通过 `MONITOR_DEVICE_URL=http://monitor-device:8062` 或等价配置直连 `monitor-device`，用于 MQTT 注册、心跳落库。
11. SVAC USB 模组物理门禁不新增 TCP/UDP 端口；启用时发布网关和终端网关需配置 `VAUTH_SVAC_MODULE_REQUIRED=true`，并至少提供 `VAUTH_SVAC_MODULE_DEVICE_PATH`、`VAUTH_SVAC_MODULE_SERIAL` 或 `VAUTH_SVAC_MODULE_VENDOR_ID`/`VAUTH_SVAC_MODULE_PRODUCT_ID` 之一，同时挂载 `/sys/bus/usb:ro` 供容器读取 USB 在位状态。
12. 发布网关 raw/crypto/image packet、流量对账查询、client-relay 状态、重组文件下载和 SVAC 文件诊断接口仍复用 `8092/tcp`，但默认由 `GATEWAY_DIAGNOSTICS_ENABLED=false` 关闭；只应在隔离排查窗口显式开启，不新增网络端口。
13. 微服务部署下发布网关和终端网关的完整 URL 覆盖项优先级最高：发布网关的 `CONTROL_PLATFORM_URL`、`MONITOR_CONTENT_URL`、`MONITOR_LOG_URL`，以及终端网关的 `CONTROL_PLATFORM_URL`、`PROBE_REPORT_URL`、`SNAPSHOT_REPORT_URL`，必须同步指向 `8063/8065/8071` 或 `8063/8062/8065` 对应微服务端口；未配置完整 URL 时才按 `MONITOR_*_PORT` 组合地址。单体/融合部署的 `8080` 保留在独立模板内。
14. `monitor-platform/deploy/stage2` 的 `--update-images` / `ctl.sh update-images` 仅加载新业务镜像并重建已有 `monitor-*` 容器，不新增 TCP/UDP 端口，也不改变 MySQL、Redis、Nacos、MinIO、Nginx、EMQX 的端口放行口径。
