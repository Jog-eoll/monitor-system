# 监管平台端口梳理

> 适用范围：`monitor-platform`、`publish-gateway`、`terminal-gateway`、`info-publish-client` 及监管服务器公共组件。
>


## 一、端口总览

| 序号 | 项目 | 最终建议端口 | 协议 | 说明 |
| --- | --- | --- | --- | --- |
| 1 | 监管平台 Web/API 对外端口 | `80` / `443` | TCP | Nginx 统一入口；HTTP `80`，启用 TLS 时 `443`。 |
| 2 | 监管平台 API 网关端口 | `8060` | TCP | 微服务模式下 `monitor-gateway` 对外/对内 API 网关端口。 |
| 3 | 监管平台单体后端端口 | `8080` | TCP | 融合/单体部署时平台 HTTP 统一端口，即 `PLATFORM_HTTP_PORT`。 |
| 4 | 监管平台后端服务端口 | `8062` - `8071` | TCP | 微服务拆分端口，详见下一节。 |
| 5 | 监管服务器 Nginx 端口 | `80` / `443` | TCP | `monitor-nginx` 监听端口；反代到 `monitor-gateway:8060` 或单体 `8080`。 |
| 6 | 内容识别 API 端口 | `8065` / `8080` / `21580` | TCP | 平台内容服务微服务端口为 `8065`；单体模式为 `8080`；本地 AI 审核模型默认端口为 `21580`。 |
| 7 | 内容识别服务 Nginx 端口 | 未单独定义 | TCP | 当前仓库未见独立内容识别 Nginx；通常走平台 Nginx `80/443` 或直接访问内容 API。 |
| 8 | 监管服务器访问内容识别服务器端口 | `8065` / `8080` / `21580` | TCP | 根据部署形态选择：微服务内容模块、单体平台或独立 AI 审核服务；平台内部通过 `LOCAL_AUDIT_*` 指向本地模型。 |
| 9 | 内容识别服务器回调监管服务器 | 有 | TCP | 内容服务会按配置访问规则、告警、设备、转发等平台服务；微服务模式涉及 `8062`、`8064`、`8066`、`8067`。 |
| 10 | MySQL | `23306` | TCP | 公共组件最终端口；容器内部仍为 `3306`。 |
| 11 | Redis | `26379` | TCP | 公共组件最终端口；容器内部仍为 `6379`。 |
| 12 | MinIO API | `29000` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `19000`，需要同步调整。 |
| 13 | MinIO Console | `29001` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `19002`，需要同步调整。 |
| 14 | Nacos HTTP | `28848` | TCP | 公共组件最终端口；仓库 stage2 旧默认值为 `18848`，需要同步调整。 |
| 15 | Nacos gRPC | `29848` | TCP | Nacos 2.x gRPC 端口；仓库旧默认值为 `19848`，需要同步调整。 |

运行态备注：2026-07-01 已将 `192.168.1.25` 现场微服务部署的业务库与 Nacos 配置库连接切到 `192.168.1.31:3306`；`23306` 仅表示本机 MySQL 容器暴露端口，当前业务服务不再以它作为数据库连接目标。
| 16 | Nacos Raft | `29849` | TCP | Nacos 2.x Raft/集群端口；仓库旧默认值为 `19849`，需要同步调整。 |
| 17 | RabbitMQ | 待确认 | TCP | 当前仓库仅见 AMQP 依赖，未见 RabbitMQ 部署配置或固定端口。若现场启用，需要由部署配置确认。 |
| 18 | SSH 运维端口 | `22` | TCP | 如现场改过 SSH 端口，以服务器实际配置为准。 |
| 19 | EMQX MQTT Broker | `1883` / `8883` / `18083` | TCP | MQTT TCP、MQTT TLS、EMQX Dashboard。公网 MQTT 场景至少放行 `1883` 或 `8883` 给设备主动连接。 |

## 二、监管平台微服务端口

| 服务 | 端口 | 用途 |
| --- | --- | --- |
| `monitor-gateway` | `8060` | API 网关、统一路由、Nginx 上游。 |
| `monitor-device` | `8062` | 设备、统一设备、注册数据落库。 |
| `monitor-ukey` | `8063` | UKey、证书校验、上线下线、心跳。 |
| `monitor-alarm` | `8064` | 告警服务。 |
| `monitor-content` | `8065` | 内容接收、内容识别、预审核接口。 |
| `monitor-rule` | `8066` | 规则服务。 |
| `monitor-forward` | `8067` | 链路编排、网关/客户端配置下发。 |
| `monitor-role` | `8068` | 角色权限。 |
| `monitor-registry-server` | `8069` | 注册中心业务服务、客户端注册/配置。 |
| `monitor-websocket` | `8070` | WebSocket 推送。 |
| `monitor-log` | `8071` | 诊断日志上报、查询。 |

## 三、网关与监管平台交互端口

| 方向 | 端口 | 主要接口/用途 | 备注 |
| --- | --- | --- | --- |
| 监管平台 -> 发布网关 | `8092` | `POST /udp-proxy/config`、链路启停/删除 | 平台下发发布链路配置。 |
| 监管平台 -> 终端网关 | `8093` | `POST /udp-proxy/config` | 内网 HTTP 直连场景下发终端网关监听和转发配置。 |
| 发布网关 -> 监管平台 UKey 服务 | `8063` 或 `8080` | `/cert/validate`、`/cert/online`、`/cert/heartbeat` | 微服务走 `8063`；单体走 `8080`。 |
| 发布网关 -> 监管平台内容服务 | `8065` 或 `8080` | 内容上报、内容检测、预审核 | 微服务走 `8065`；单体走 `8080`。 |
| 发布网关 -> 监管平台设备服务 | `8062` 或 `8080` | 设备注册、设备状态 | 微服务走 `8062`；单体走 `8080`。 |
| 发布网关 -> 监管平台日志服务 | `8071` 或 `8080` | 诊断日志上报 | 微服务走 `8071`；单体走 `8080`。 |
| 发布网关 -> EMQX Broker | `1883` 或 `8883` | MQTT 注册、心跳、回执、下行命令 | 设备主动连接公网 Broker，平台不直接访问设备内网 IP。 |
| 终端网关 -> 监管平台 UKey/设备/内容服务 | `8080` 或 `8063/8062/8065` | 证书校验、状态上报、截图回传 | 终端网关配置里单体默认走 `8080`。 |
| 终端网关 -> EMQX Broker | `1883` 或 `8883` | MQTT 注册、心跳、回执、下行命令 | 与发布网关一致由 `mqtt-agent.*` 配置控制；不新增终端网关入站端口。 |
| mDNS 自动发现 | `5353` | UDP | 如果启用自动发现，需要同网段放行 `5353/udp`。 |

## 四、信发客户端与监管平台交互端口

| 方向 | 端口 | 主要接口/用途 | 备注 |
| --- | --- | --- | --- |
| `info-publish-client` 对外 HTTP | `7081` | 接收平台/发布网关配置下发 | 现场配置和安装日志均使用 `7081`；源码默认 `7080`。 |
| 监管平台 -> `info-publish-client` | `7081` | `POST /udp-proxy/config` | 下发发布网关、终端网关、情报板、MAC 等链路配置。 |
| 监管平台 -> `info-publish-client` | `7081` | `PUT /udp-proxy/chain/{chainId}/status`、`DELETE /udp-proxy/chain/{chainId}` | 链路启停和删除。 |
| 发布网关 -> `info-publish-client` | `7081` | 中转 `POST /udp-proxy/config`，按需查询 `/security/traffic/stats` 做流量对账 | `publish-gateway/deploy/application.yml` 已是 `CLIENT_PORT=7081`；源码默认仍有 `7080`，部署需覆盖；发布网关不再逐包回调 `/security/validate-source`。 |
| `info-publish-client` -> 监管平台注册服务 | `8080` 或 `8069` | `POST /registry/register`、`GET /registry/client-config/{clientId}` | 现场配置示例为 `192.168.1.233:8080`；微服务直连可走 `8069` 或平台统一入口。 |
| `info-publish-client` -> 监管平台 UKey 服务 | `8080` 或 `8063` | `/cert/validate`、`/cert/online`、`/cert/heartbeat`、`/api/client/ukey/register` | 由远程配置中的 `monitorPlatformUrl` 或本地 `monitor-platform.url` 决定。 |
| `info-publish-client` -> MySQL | `23306` 或现场外部库端口 | 注册配置 HTTP 失败时兜底读取 `registry_client_config` | 当前 `192.168.1.25` 现场配置指向 `192.168.1.31:3306`；通用部署可继续按公共组件口径使用 `23306`。 |
| `info-publish-client` -> 内容预审核 | `8065` / `8080` | `/content/detection/pre-audit` | 客户端仍调用平台内容服务；平台内容服务再按 `LOCAL_AUDIT_*` 配置访问 `21580` 本地审核模型。 |

## 五、信发客户端与网关/信发板端口

| 方向 | 端口 | 协议 | 用途 |
| --- | --- | --- | --- |
| `info-publish-client` -> 发布网关 | `8092` | TCP | 安全投递、发布网关健康检查、转发控制。 |
| 发布网关 UDP 中继 | `18092` | UDP | `info-publish-client` 到发布网关/透明转发链路的客户端 UDP 中继端口。 |
| 信发板常见业务端口 | `9520` | UDP/TCP | 默认目标端口，具体以设备配置为准。 |
| Nova/厂商协议端口 | `16606`、`16602`、`16601`、`16611`、`19999` | UDP/TCP | 源码和运行日志中出现的厂商/设备端口，按现场设备协议确认。 |

## 六、防火墙放行建议

| 服务器/主机 | 入方向建议放行 | 出方向建议放行 |
| --- | --- | --- |
| 监管服务器 | `80/tcp`、`443/tcp`、`8060-8071/tcp`、`8080/tcp`、`1883/tcp` 或 `8883/tcp`、`18083/tcp`、`23306/tcp`、`26379/tcp`、`28848/tcp`、`29848/tcp`、`29849/tcp`、`29000/tcp`、`29001/tcp`、`22/tcp` | 到发布网关 `8092/tcp`、终端网关 `8093/tcp`、信发客户端 `7081/tcp`、平台内容识别服务 `8065/8080/tcp`；若本地审核模型独立部署，还需平台内容服务访问 `21580/tcp`。公网 MQTT 场景由设备主动连入 EMQX。 |
| 发布网关服务器 | `8092/tcp`、`18092/udp`、必要的厂商 UDP/TCP 端口 | 到监管平台 `8062/8063/8065/8071/tcp` 或单体 `8080/tcp`，到 EMQX `1883/8883/tcp`，到信发客户端 `7081/tcp`，到终端网关业务端口；启用 SVAC USB 模组门禁时，本机需容器只读访问 `/sys/bus/usb`，不新增网络端口。 |
| 终端网关服务器 | `8093/tcp`、终端侧业务 UDP/TCP 端口 | 到监管平台 `8080/tcp` 或 `8062/8063/8065/tcp`，到 EMQX `1883/8883/tcp`，到 MinIO `29000/tcp`；启用 SVAC USB 模组门禁时，本机需容器只读访问 `/sys/bus/usb`，不新增网络端口。 |
| 信发客户端主机 | `7081/tcp`、`18092/udp`、信发板业务端口 | 到监管平台 `8080/8069/8063/8065/tcp`，到发布网关 `8092/tcp`，到 MySQL `23306/tcp`。 |

## 七、需要同步调整或确认的项

1. `MinIO`、`Nacos` 在 `monitor-platform/deploy/stage2` 中仍保留 `19000/19002`、`18848/19848/19849` 旧默认值；若按公共组件前缀 `2` 的最终口径部署，需要同步改为 `29000/29001`、`28848/29848/29849`。
2. `info-publish-client` 源码默认 `server.port=7080`，但现场配置、安装日志和发布网关部署配置应统一为 `7081`。
3. `publish-gateway/gateway-udp-proxy/src/main/resources/application.yml` 中 `CLIENT_PORT` 默认仍是 `7080`；实际部署应使用 `publish-gateway/deploy/application.yml` 或环境变量覆盖为 `7081`。
4. `info-publish-client` 内容发布链路不新增端口，但需要按现场网络配置超时：`HTTP_CLIENT_CONNECT_TIMEOUT_MS` 默认 `10000`，`HTTP_CLIENT_READ_TIMEOUT_MS` 默认 `120000`，`CONTENT_PUBLISH_DOWNLOAD_TIMEOUT_MS` 默认 `120000`；青松或前端调用 `7081` 内容发布接口时，HTTP client 超时不应低于业务等待时间。
5. `RabbitMQ` 当前仓库没有部署文件和连接配置，不能直接作为必开端口；如果现场确实启用，需要补充 broker 地址、AMQP 端口、管理端口和访问方向。
6. SSH 默认为 `22/tcp`，如果服务器安全基线改过端口，应以 `/etc/ssh/sshd_config` 和防火墙实际规则为准。
7. Nginx 微服务入口统一识别后端服务前缀并转发到 `monitor-gateway:8060`，支持无 `/api` 的业务路径（如 `/content/**`、`/log/**`、`/upgrade/**`），同时兼容 `/api/**` 并在转发前剥离 `/api` 前缀；具体服务归属仍由网关路由决定。
8. MQTT Broker Auth/Webhook 使用平台现有 HTTP 端口承载：微服务走 `monitor-forward:8067`，单体走 `8080`，不新增独立平台 HTTP 端口。
9. 云端 MQTT 最小部署如禁用 Nacos 服务发现，`monitor-forward` 仍需通过 `MONITOR_DEVICE_URL=http://monitor-device:8062` 或等价配置直连 `monitor-device`，用于 MQTT 注册、心跳落库。
10. SVAC USB 模组物理门禁不新增 TCP/UDP 端口；启用时发布网关和终端网关需配置 `VAUTH_SVAC_MODULE_REQUIRED=true`，并至少提供 `VAUTH_SVAC_MODULE_DEVICE_PATH`、`VAUTH_SVAC_MODULE_SERIAL` 或 `VAUTH_SVAC_MODULE_VENDOR_ID`/`VAUTH_SVAC_MODULE_PRODUCT_ID` 之一，同时挂载 `/sys/bus/usb:ro` 供容器读取 USB 在位状态。
11. 发布网关 raw/crypto/image packet、流量对账查询、client-relay 状态、重组文件下载和 SVAC 文件诊断接口仍复用 `8092/tcp`，但默认由 `GATEWAY_DIAGNOSTICS_ENABLED=false` 关闭；只应在隔离排查窗口显式开启，不新增网络端口。
12. 微服务部署下发布网关和终端网关的 UKey 认证地址必须显式指向 `monitor-ukey:8063`。如果部署包存在 `CONTROL_PLATFORM_URL`、`PROBE_REPORT_URL`、`SNAPSHOT_REPORT_URL` 等覆盖项，需要同步改为 `8063/8062/8065` 对应微服务端口；否则即使 `MONITOR_UKEY_PORT=8063` 已配置，运行时仍可能访问旧的 `8080` 单体入口。
